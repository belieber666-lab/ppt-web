services:
  - type: web
    name: ppt-api
    runtime: python
    rootDir: apps/python-api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
    plan: free
